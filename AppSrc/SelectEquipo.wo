// C:\DataFlex Projects\futbol\AppSrc\SelectEquipo.wo
// Lista de equipos
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg
Use ZoomEquipo.wo

Use cligaDataDictionary.dd
Use cequipoDataDictionary.dd

Object oSelectEquipo is a cWebView
    Set psCaption to "Lista de equipos"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtSelect
    Set piColumnCount to 12
    Set pbShowCaption to False
    Set piMaxWidth to 1024

    Object oliga_DD is a cligaDataDictionary
    End_Object

    Object oequipo_DD is a cequipoDataDictionary
        Set DDO_Server to oliga_DD
    End_Object

    Set Main_DD to oequipo_DD
    Set Server  to oequipo_DD

    Object oList is a cWebList
        Set piColumnSpan to 0
        Set psCSSClass to "MobileList"
        Set pbServerOnRowClick to True
        Set pbFillHeight to True
        Set pbShowHeader to False
        Set piSortColumn to 0

        Object oequipoNombre is a cWebColumn
            Entry_Item equipo.Nombre
            Set psCaption to "equipo.Nombre"
            Set piWidth to 1491
        End_Object

        Object oDetailButton is a cWebColumnButton
            Set psCaption to "btn"
            Set piWidth to 45
            Set pbFixedWidth to True
            Set pbResizable to False
            Set piListRowSpan to 2
            Set psBtnCssClass to "WebButtonIcon WebIcon_Info"
            Set peAlign to alignRight

            WebRegisterPath ntNavigateForward oZoomEquipo

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bReadOnly
            End_Procedure

        End_Object

        Object oligaID is a cWebColumn
            Entry_Item liga.ID
            Set psCaption to "equipo.LigaID"
            Set piWidth to 124
            Set pbNewLine to True
        End_Object

        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData
            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    // FROM PARENT: vengo de ZoomLiga
                    // El usuario selecciona un equipo y vuelvo al padre
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    // FROM CHILD: vengo de un hijo que quiere elegir su padre
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    // FROM MAIN: mismo DD
                    Send NavigateClose Self
                    Case Break

                Case Else
                    // UNDEFINED: vengo del Dashboard, no hacer nada
                    // El boton i es el que navega al Zoom

            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            Move True to NavigateData.bReadOnly
        End_Procedure

    End_Object

    Object oActionGroup is a cWebMenuGroup

        Object oSearch is a cWebMenuItem
            Set psCSSClass to "WebPromptMenuItem"
            Set psCaption to "Search"

            Procedure OnClick
                Send Search of oList
            End_Procedure

        End_Object

        Object oNewButton is a cWebMenuItem
            Set psCSSClass to "WebClearMenuItem"
            Set psCaption to "New"

            WebRegisterPath ntNavigateForward oZoomEquipo

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bNewRecord
            End_Procedure

        End_Object
    End_Object

    // EVENTO 2 - OnNavigateForward (puerta de entrada a SelectEquipo)
    // Cuando vengo FROM PARENT (desde ZoomLiga):
    // la barra libre ya ha filtrado los equipos de esa liga
    // Solo tengo que ocultar el New y mostrar un mensaje
    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        WebSet pbRender of oNewButton to True
        WebSet pbRender of oDetailButton to True

        Case Begin
            Case (NavigateData.eNavigateType = nfFromParent)
                // FROM PARENT: vengo de ZoomLiga
                // La barra libre ha filtrado los equipos de esa liga automaticamente
                // Oculto New porque solo estoy consultando, no creando
                Send ShowInfoBox "FROM PARENT|He llegado al SelectEquipo desde ZoomLiga|La barra libre ha filtrado los equipos de esta liga|New Button ocultado" "Evento 2: OnNavigateForward"
                WebSet pbRender of oNewButton to False
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                // FROM CHILD: vengo de un hijo que quiere elegir su equipo padre
                // Oculto New y Detail - solo seleccionar
                Send ShowInfoBox "FROM CHILD|He llegado al SelectEquipo desde un hijo|Solo seleccionar equipo|New y Detail ocultados" "Evento 2: OnNavigateForward"
                WebSet pbRender of oNewButton to False
                WebSet pbRender of oDetailButton to False
                Case Break

            Case (NavigateData.eNavigateType = nfFromMain)
                Send ShowInfoBox "FROM MAIN|Mismo DD|Detail ocultado" "Evento 2: OnNavigateForward"
                WebSet pbRender of oDetailButton to False
                Case Break

            Case Else
                // UNDEFINED: vengo del Dashboard, muestro todo

        Case End

    End_Procedure

End_Object